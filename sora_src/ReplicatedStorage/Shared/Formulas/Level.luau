--- Level = Exp required
local expRequiredForLevel = {} :: { number }
local formulas = {}

function formulas.expToLevel(exp: number): number
    local level: number = 1
    return level
end

return table.freeze(formulas)