
local RepS = game:GetService("ReplicatedStorage")

local Signal = require(RepS.HarukaShared.Shared.Signal)

return {
    ChristmasTheSickMan = Signal.new(),
    ChristmasInterShovel = Signal.new(),
    ChristmasCompletedSnuwaz = Signal.new(),
}