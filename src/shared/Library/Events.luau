--!native
--!optimize 2
--!nocheck
--!nolint
--#selene: allow(unused_variable, global_usage)
-- Client generated by Zap v0.6.20 (https://github.com/red-blox/zap)
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local RunService = game:GetService("RunService")

local outgoing_buff: buffer
local outgoing_used: number
local outgoing_size: number
local outgoing_inst: { Instance }
local outgoing_apos: number
local outgoing_ids: { number }

local incoming_buff: buffer
local incoming_read: number
local incoming_inst: { Instance }
local incoming_ipos: number
local incoming_ids: { number }

-- thanks to https://dom.rojo.space/binary.html#cframe
local CFrameSpecialCases = {
	CFrame.Angles(0, 0, 0),
	CFrame.Angles(math.rad(90), 0, 0),
	CFrame.Angles(0, math.rad(180), math.rad(180)),
	CFrame.Angles(math.rad(-90), 0, 0),
	CFrame.Angles(0, math.rad(180), math.rad(90)),
	CFrame.Angles(0, math.rad(90), math.rad(90)),
	CFrame.Angles(0, 0, math.rad(90)),
	CFrame.Angles(0, math.rad(-90), math.rad(90)),
	CFrame.Angles(math.rad(-90), math.rad(-90), 0),
	CFrame.Angles(0, math.rad(-90), 0),
	CFrame.Angles(math.rad(90), math.rad(-90), 0),
	CFrame.Angles(0, math.rad(90), math.rad(180)),
	CFrame.Angles(0, math.rad(-90), math.rad(180)),
	CFrame.Angles(0, math.rad(180), math.rad(0)),
	CFrame.Angles(math.rad(-90), math.rad(-180), math.rad(0)),
	CFrame.Angles(0, math.rad(0), math.rad(180)),
	CFrame.Angles(math.rad(90), math.rad(180), math.rad(0)),
	CFrame.Angles(0, math.rad(0), math.rad(-90)),
	CFrame.Angles(0, math.rad(-90), math.rad(-90)),
	CFrame.Angles(0, math.rad(-180), math.rad(-90)),
	CFrame.Angles(0, math.rad(90), math.rad(-90)),
	CFrame.Angles(math.rad(90), math.rad(90), 0),
	CFrame.Angles(0, math.rad(90), 0),
	CFrame.Angles(math.rad(-90), math.rad(90), 0),
}

local function alloc(len: number)
	if outgoing_used + len > outgoing_size then
		while outgoing_used + len > outgoing_size do
			outgoing_size = outgoing_size * 2
		end

		local new_buff = buffer.create(outgoing_size)
		buffer.copy(new_buff, 0, outgoing_buff, 0, outgoing_used)

		outgoing_buff = new_buff
	end

	outgoing_apos = outgoing_used
	outgoing_used = outgoing_used + len

	return outgoing_apos
end

local function read(len: number)
	local pos = incoming_read
	incoming_read = incoming_read + len

	return pos
end

local function save()
	return {
		buff = outgoing_buff,
		used = outgoing_used,
		size = outgoing_size,
		inst = outgoing_inst,
		outgoing_ids = outgoing_ids,
		incoming_ids = incoming_ids,
	}
end

local function load(data: {
	buff: buffer,
	used: number,
	size: number,
	inst: { Instance },
	outgoing_ids: { number },
	incoming_ids: { number },
})
	outgoing_buff = data.buff
	outgoing_used = data.used
	outgoing_size = data.size
	outgoing_inst = data.inst
	outgoing_ids = data.outgoing_ids
	incoming_ids = data.incoming_ids
end

local function load_empty()
	outgoing_buff = buffer.create(64)
	outgoing_used = 0
	outgoing_size = 64
	outgoing_inst = {}
	outgoing_ids = {}
	incoming_ids = {}
end

load_empty()

local types = {}

local polling_queues_reliable = {}
local polling_queues_unreliable = {}
if not RunService:IsRunning() then
	local noop = function() end
	return table.freeze({
		SendEvents = noop,
		RequestToChooseWhichSlotToPlay = table.freeze({
			Fire = noop
		}),
		RequestToEraseSaveFile = table.freeze({
			Fire = noop
		}),
		ApplyFoundStatusToEnemy = table.freeze({
			On = noop
		}),
		PlaceOrb = table.freeze({
			On = noop
		}),
		RequestToEndNPCChat = table.freeze({
			Fire = noop
		}),
		EnterNPCChat = table.freeze({
			On = noop
		}),
		OpenCastTimeBar = table.freeze({
			On = noop
		}),
		PlayGotHitScreenFX = table.freeze({
			On = noop
		}),
		PlayStatPoweredScreenFX = table.freeze({
			On = noop
		}),
		PlayHealScreenFX = table.freeze({
			On = noop
		}),
		PlayItemDropFX = table.freeze({
			On = noop
		}),
		OpenEXPAwardBar = table.freeze({
			On = noop
		}),
		ShowDMGNumber = table.freeze({
			On = noop
		}),
		ShowText = table.freeze({
			On = noop
		}),
		PlayHitStatusVFX = table.freeze({
			On = noop
		}),
		SpawnItemDrop = table.freeze({
			On = noop
		}),
		OpenChestClientTween = table.freeze({
			On = noop
		}),
		RequestToDMGEnemy = table.freeze({
			Fire = noop
		}),
		RequestToDMGPlayer = table.freeze({
			Fire = noop
		}),
		SpawnATKs = table.freeze({
			On = noop
		}),
		SpawnUniATKs = table.freeze({
			On = noop
		}),
		RequestToCastItem_Request = table.freeze({
			Fire = noop
		}),
		RequestToCastItem_Response = table.freeze({
			On = noop
		}),
		RequestToCastClassUlt = table.freeze({
			Fire = noop
		}),
		PlayClassSPScene = table.freeze({
			On = noop
		}),
		FaceToTargetingEnemy = table.freeze({
			On = noop
		}),
		OpenMarketUI = table.freeze({
			On = noop
		}),
		RequestToPickupItemDrops = table.freeze({
			Fire = noop
		}),
		RequestToEquipItem = table.freeze({
			Fire = noop
		}),
		RequestToPurchaseItem = table.freeze({
			Fire = noop
		}),
		RequestToSellItem = table.freeze({
			Fire = noop
		}),
		RequestToAddPoint = table.freeze({
			Fire = noop
		}),
		PlayAnimation = table.freeze({
			On = noop
		}),
		RequestToDMGEnemyBySkillProjectile = table.freeze({
			Fire = noop
		}),
		PlayAnimForEnemy = table.freeze({
			On = noop
		}),
		RequestToStartCombat = table.freeze({
			Fire = noop
		}),
		RequestToResetPoint = table.freeze({
			Fire = noop
		}),
		RequestToSellAllItems = table.freeze({
			Fire = noop
		}),
		OpenCraftingUI = table.freeze({
			On = noop
		}),
		RequestToCraftItem = table.freeze({
			Fire = noop
		}),
		ApplyHighlightForEntity = table.freeze({
			On = noop
		}),
		RequestToSaveSettings = table.freeze({
			Fire = noop
		}),
		Spell_ExtraJumpSupport = table.freeze({
			On = noop
		}),
		CLASS_RollNewResonateNumForEnemy = table.freeze({
			On = noop
		}),
		CLASS_RollNewResonateNumForPlayer = table.freeze({
			On = noop
		}),
		RequestToForceReset = table.freeze({
			Fire = noop
		}),
		RequestToRejoinGame = table.freeze({
			Fire = noop
		}),
		PlaySound = table.freeze({
			On = noop
		}),
		PlayClientTween = table.freeze({
			On = noop
		}),
		PlayCameraCutscene = table.freeze({
			On = noop
		}),
		CreateNotify = table.freeze({
			On = noop
		}),
		CreateSystemNotify = table.freeze({
			On = noop
		}),
		CreateCenterNotify = table.freeze({
			On = noop
		}),
		CreateFSNotify = table.freeze({
			On = noop
		}),
		CreateCircularFade = table.freeze({
			On = noop
		}),
	}) :: Events
end
if RunService:IsServer() then
	error("Cannot use the client module on the server!")
end
local remotes = ReplicatedStorage:WaitForChild("EternalLand")

local reliable = remotes:WaitForChild("Hyacine_RELIABLE")
assert(reliable:IsA("RemoteEvent"), "Expected Hyacine_RELIABLE to be a RemoteEvent")


local function SendEvents()
	if outgoing_used ~= 0 then
		local buff = buffer.create(outgoing_used)
		buffer.copy(buff, 0, outgoing_buff, 0, outgoing_used)

		reliable:FireServer(buff, outgoing_inst)

		outgoing_buff = buffer.create(64)
		outgoing_used = 0
		outgoing_size = 64
		table.clear(outgoing_inst)
	end
end

RunService.Heartbeat:Connect(SendEvents)

local reliable_events = table.create(35)
local reliable_event_queue: { [number]: { any } } = table.create(35)
reliable_events[0] = {}
reliable_event_queue[0] = {}
reliable_events[1] = {}
reliable_event_queue[1] = {}
reliable_events[2] = {}
reliable_event_queue[2] = {}
reliable_events[3] = {}
reliable_event_queue[3] = {}
reliable_events[4] = {}
reliable_event_queue[4] = 0
reliable_events[5] = {}
reliable_event_queue[5] = 0
reliable_events[6] = {}
reliable_event_queue[6] = 0
reliable_events[7] = {}
reliable_event_queue[7] = {}
reliable_events[8] = {}
reliable_event_queue[8] = {}
reliable_events[9] = {}
reliable_event_queue[9] = {}
reliable_events[10] = {}
reliable_event_queue[10] = {}
reliable_events[11] = {}
reliable_event_queue[11] = {}
reliable_events[12] = {}
reliable_event_queue[12] = {}
reliable_events[13] = {}
reliable_event_queue[13] = {}
reliable_events[14] = {}
reliable_event_queue[14] = {}
reliable_events[15] = {}
reliable_event_queue[15] = {}
reliable_events[16] = {}
reliable_event_queue[16] = {}
reliable_events[17] = {}
reliable_event_queue[17] = {}
reliable_events[18] = {}
reliable_event_queue[18] = 0
reliable_events[19] = {}
reliable_event_queue[19] = {}
reliable_events[20] = {}
reliable_event_queue[20] = {}
reliable_events[21] = {}
reliable_event_queue[21] = {}
reliable_events[22] = {}
reliable_event_queue[22] = 0
reliable_events[23] = {}
reliable_event_queue[23] = {}
reliable_events[24] = {}
reliable_event_queue[24] = 0
reliable_events[25] = {}
reliable_event_queue[25] = {}
reliable_events[26] = {}
reliable_event_queue[26] = {}
reliable_events[27] = {}
reliable_event_queue[27] = {}
reliable_events[28] = {}
reliable_event_queue[28] = {}
reliable_events[29] = {}
reliable_event_queue[29] = {}
reliable_events[30] = {}
reliable_event_queue[30] = {}
reliable_events[31] = {}
reliable_event_queue[31] = {}
reliable_events[32] = {}
reliable_event_queue[32] = {}
reliable_events[33] = {}
reliable_event_queue[33] = {}
reliable_events[34] = {}
reliable_event_queue[34] = {}
reliable.OnClientEvent:Connect(function(buff, inst)
	incoming_buff = buff
	incoming_inst = inst
	incoming_read = 0
	incoming_ipos = 0
	local len = buffer.len(buff)
	while incoming_read < len do
		local id = buffer.readu8(buff, read(1))
		if id == 0 then
			local value
			value = {}
			if buffer.readu8(incoming_buff, read(1)) == 1 then
				incoming_ipos = incoming_ipos + 1
				value["Enemy"] = incoming_inst[incoming_ipos]
				assert(value["Enemy"] == nil or value["Enemy"]:IsA("Model"), "received instance is not of the Model class!")
			else
				value["Enemy"] = nil
			end
			if reliable_events[0][1] then
				for _, cb in reliable_events[0] do
					task.spawn(cb, value)
				end
			else
				table.insert(reliable_event_queue[0], value)
				if #reliable_event_queue[0] > 64 then
					warn(`[ZAP] {#reliable_event_queue[0]} events in queue for ApplyFoundStatusToEnemy. Did you forget to attach a listener?`)
				end
			end
		elseif id == 1 then
			local value
			value = {}
			local len_1 = buffer.readu16(incoming_buff, read(2))
			value["OrbId"] = buffer.readstring(incoming_buff, read(len_1), len_1)
			local len_2 = buffer.readu16(incoming_buff, read(2))
			value["OrbUniqueId"] = buffer.readstring(incoming_buff, read(len_2), len_2)
			value["OrbDuration"] = buffer.readu8(incoming_buff, read(1))
			value["OrbLocationPos"] = Vector3.new(buffer.readf32(incoming_buff, read(4)), buffer.readf32(incoming_buff, read(4)), buffer.readf32(incoming_buff, read(4)))
			if reliable_events[1][1] then
				for _, cb in reliable_events[1] do
					task.spawn(cb, value)
				end
			else
				table.insert(reliable_event_queue[1], value)
				if #reliable_event_queue[1] > 64 then
					warn(`[ZAP] {#reliable_event_queue[1]} events in queue for PlaceOrb. Did you forget to attach a listener?`)
				end
			end
		elseif id == 2 then
			local value
			value = {}
			local len_3 = buffer.readu16(incoming_buff, read(2))
			value["NPCId"] = buffer.readstring(incoming_buff, read(len_3), len_3)
			if reliable_events[2][1] then
				for _, cb in reliable_events[2] do
					task.spawn(cb, value)
				end
			else
				table.insert(reliable_event_queue[2], value)
				if #reliable_event_queue[2] > 64 then
					warn(`[ZAP] {#reliable_event_queue[2]} events in queue for EnterNPCChat. Did you forget to attach a listener?`)
				end
			end
		elseif id == 3 then
			local value
			value = {}
			value["Duration"] = buffer.readu8(incoming_buff, read(1))
			if reliable_events[3][1] then
				for _, cb in reliable_events[3] do
					task.spawn(cb, value)
				end
			else
				table.insert(reliable_event_queue[3], value)
				if #reliable_event_queue[3] > 64 then
					warn(`[ZAP] {#reliable_event_queue[3]} events in queue for OpenCastTimeBar. Did you forget to attach a listener?`)
				end
			end
		elseif id == 4 then
			local value
			if reliable_events[4][1] then
				for _, cb in reliable_events[4] do
					task.spawn(cb, value)
				end
			else
				reliable_event_queue[4] += 1
				if reliable_event_queue[4] > 16 then
					warn(`[ZAP] {reliable_event_queue[4]} events in queue for PlayGotHitScreenFX. Did you forget to attach a listener?`)
				end
			end
		elseif id == 5 then
			local value
			if reliable_events[5][1] then
				for _, cb in reliable_events[5] do
					task.spawn(cb, value)
				end
			else
				reliable_event_queue[5] += 1
				if reliable_event_queue[5] > 16 then
					warn(`[ZAP] {reliable_event_queue[5]} events in queue for PlayStatPoweredScreenFX. Did you forget to attach a listener?`)
				end
			end
		elseif id == 6 then
			local value
			if reliable_events[6][1] then
				for _, cb in reliable_events[6] do
					task.spawn(cb, value)
				end
			else
				reliable_event_queue[6] += 1
				if reliable_event_queue[6] > 16 then
					warn(`[ZAP] {reliable_event_queue[6]} events in queue for PlayHealScreenFX. Did you forget to attach a listener?`)
				end
			end
		elseif id == 7 then
			local value
			value = {}
			local len_4 = buffer.readu16(incoming_buff, read(2))
			value["ItemId"] = buffer.readstring(incoming_buff, read(len_4), len_4)
			value["Amount"] = buffer.readu16(incoming_buff, read(2))
			if reliable_events[7][1] then
				for _, cb in reliable_events[7] do
					task.spawn(cb, value)
				end
			else
				table.insert(reliable_event_queue[7], value)
				if #reliable_event_queue[7] > 64 then
					warn(`[ZAP] {#reliable_event_queue[7]} events in queue for PlayItemDropFX. Did you forget to attach a listener?`)
				end
			end
		elseif id == 8 then
			local value
			value = {}
			value["EXPGained"] = buffer.readu16(incoming_buff, read(2))
			if reliable_events[8][1] then
				for _, cb in reliable_events[8] do
					task.spawn(cb, value)
				end
			else
				table.insert(reliable_event_queue[8], value)
				if #reliable_event_queue[8] > 64 then
					warn(`[ZAP] {#reliable_event_queue[8]} events in queue for OpenEXPAwardBar. Did you forget to attach a listener?`)
				end
			end
		elseif id == 9 then
			local value
			value = {}
			if buffer.readu8(incoming_buff, read(1)) == 1 then
				incoming_ipos = incoming_ipos + 1
				value["DMGTarget"] = incoming_inst[incoming_ipos]
				assert(value["DMGTarget"] == nil or value["DMGTarget"]:IsA("Model"), "received instance is not of the Model class!")
			else
				value["DMGTarget"] = nil
			end
			local len_5 = buffer.readu16(incoming_buff, read(2))
			value["DMGNumber"] = buffer.readstring(incoming_buff, read(len_5), len_5)
			value["DMGColor"] = Color3.fromRGB(buffer.readu8(incoming_buff, read(1)), buffer.readu8(incoming_buff, read(1)), buffer.readu8(incoming_buff, read(1)))
			if reliable_events[9][1] then
				for _, cb in reliable_events[9] do
					task.spawn(cb, value)
				end
			else
				table.insert(reliable_event_queue[9], value)
				if #reliable_event_queue[9] > 64 then
					warn(`[ZAP] {#reliable_event_queue[9]} events in queue for ShowDMGNumber. Did you forget to attach a listener?`)
				end
			end
		elseif id == 10 then
			local value
			value = {}
			if buffer.readu8(incoming_buff, read(1)) == 1 then
				incoming_ipos = incoming_ipos + 1
				value["Target"] = incoming_inst[incoming_ipos]
				assert(value["Target"] == nil or value["Target"]:IsA("Model"), "received instance is not of the Model class!")
			else
				value["Target"] = nil
			end
			local len_6 = buffer.readu16(incoming_buff, read(2))
			value["Text"] = buffer.readstring(incoming_buff, read(len_6), len_6)
			value["TextColor"] = Color3.fromRGB(buffer.readu8(incoming_buff, read(1)), buffer.readu8(incoming_buff, read(1)), buffer.readu8(incoming_buff, read(1)))
			if reliable_events[10][1] then
				for _, cb in reliable_events[10] do
					task.spawn(cb, value)
				end
			else
				table.insert(reliable_event_queue[10], value)
				if #reliable_event_queue[10] > 64 then
					warn(`[ZAP] {#reliable_event_queue[10]} events in queue for ShowText. Did you forget to attach a listener?`)
				end
			end
		elseif id == 11 then
			local value
			value = {}
			if buffer.readu8(incoming_buff, read(1)) == 1 then
				incoming_ipos = incoming_ipos + 1
				value["Target"] = incoming_inst[incoming_ipos]
				assert(value["Target"] == nil or value["Target"]:IsA("Model"), "received instance is not of the Model class!")
			else
				value["Target"] = nil
			end
			if reliable_events[11][1] then
				for _, cb in reliable_events[11] do
					task.spawn(cb, value)
				end
			else
				table.insert(reliable_event_queue[11], value)
				if #reliable_event_queue[11] > 64 then
					warn(`[ZAP] {#reliable_event_queue[11]} events in queue for PlayHitStatusVFX. Did you forget to attach a listener?`)
				end
			end
		elseif id == 12 then
			local value
			value = {}
			local len_7 = buffer.readu16(incoming_buff, read(2))
			value["ItemUniqueId"] = buffer.readstring(incoming_buff, read(len_7), len_7)
			local len_8 = buffer.readu16(incoming_buff, read(2))
			value["ItemId"] = buffer.readstring(incoming_buff, read(len_8), len_8)
			value["ItemAmount"] = buffer.readu8(incoming_buff, read(1))
			value["ItemDropLocationPos"] = Vector3.new(buffer.readf32(incoming_buff, read(4)), buffer.readf32(incoming_buff, read(4)), buffer.readf32(incoming_buff, read(4)))
			if reliable_events[12][1] then
				for _, cb in reliable_events[12] do
					task.spawn(cb, value)
				end
			else
				table.insert(reliable_event_queue[12], value)
				if #reliable_event_queue[12] > 64 then
					warn(`[ZAP] {#reliable_event_queue[12]} events in queue for SpawnItemDrop. Did you forget to attach a listener?`)
				end
			end
		elseif id == 13 then
			local value
			value = {}
			if buffer.readu8(incoming_buff, read(1)) == 1 then
				incoming_ipos = incoming_ipos + 1
				value["Chest"] = incoming_inst[incoming_ipos]
				assert(value["Chest"] == nil or value["Chest"]:IsA("Model"), "received instance is not of the Model class!")
			else
				value["Chest"] = nil
			end
			if reliable_events[13][1] then
				for _, cb in reliable_events[13] do
					task.spawn(cb, value)
				end
			else
				table.insert(reliable_event_queue[13], value)
				if #reliable_event_queue[13] > 64 then
					warn(`[ZAP] {#reliable_event_queue[13]} events in queue for OpenChestClientTween. Did you forget to attach a listener?`)
				end
			end
		elseif id == 14 then
			local value
			value = {}
			local len_9 = buffer.readu16(incoming_buff, read(2))
			value["ProjectileId"] = buffer.readstring(incoming_buff, read(len_9), len_9)
			local len_10 = buffer.readu16(incoming_buff, read(2))
			value["ProjectileUniqueId"] = buffer.readstring(incoming_buff, read(len_10), len_10)
			local pos_1 = Vector3.new(buffer.readf32(incoming_buff, read(4)), buffer.readf32(incoming_buff, read(4)), buffer.readf32(incoming_buff, read(4)))
			local axisangle_1 = Vector3.new(buffer.readf32(incoming_buff, read(4)), buffer.readf32(incoming_buff, read(4)), buffer.readf32(incoming_buff, read(4)))
			local angle_1 = axisangle_1.Magnitude
			if angle_1 ~= 0 then
				value["ProjectileCF"] = CFrame.fromAxisAngle(axisangle_1, angle_1) + pos_1
			else
				value["ProjectileCF"] = CFrame.new(pos_1)
			end
			if buffer.readu8(incoming_buff, read(1)) == 1 then
				incoming_ipos = incoming_ipos + 1
				value["ProjectileOwner"] = incoming_inst[incoming_ipos]
				assert(value["ProjectileOwner"] == nil or value["ProjectileOwner"]:IsA("Model"), "received instance is not of the Model class!")
			else
				value["ProjectileOwner"] = nil
			end
			if reliable_events[14][1] then
				for _, cb in reliable_events[14] do
					task.spawn(cb, value)
				end
			else
				table.insert(reliable_event_queue[14], value)
				if #reliable_event_queue[14] > 64 then
					warn(`[ZAP] {#reliable_event_queue[14]} events in queue for SpawnATKs. Did you forget to attach a listener?`)
				end
			end
		elseif id == 15 then
			local value
			value = {}
			local len_11 = buffer.readu16(incoming_buff, read(2))
			value["ProjectileId"] = buffer.readstring(incoming_buff, read(len_11), len_11)
			local len_12 = buffer.readu16(incoming_buff, read(2))
			value["ProjectileUniqueId"] = buffer.readstring(incoming_buff, read(len_12), len_12)
			local pos_2 = Vector3.new(buffer.readf32(incoming_buff, read(4)), buffer.readf32(incoming_buff, read(4)), buffer.readf32(incoming_buff, read(4)))
			local axisangle_2 = Vector3.new(buffer.readf32(incoming_buff, read(4)), buffer.readf32(incoming_buff, read(4)), buffer.readf32(incoming_buff, read(4)))
			local angle_2 = axisangle_2.Magnitude
			if angle_2 ~= 0 then
				value["ProjectileCF"] = CFrame.fromAxisAngle(axisangle_2, angle_2) + pos_2
			else
				value["ProjectileCF"] = CFrame.new(pos_2)
			end
			if buffer.readu8(incoming_buff, read(1)) == 1 then
				incoming_ipos = incoming_ipos + 1
				value["ProjectileOwner"] = incoming_inst[incoming_ipos]
				assert(value["ProjectileOwner"] == nil or value["ProjectileOwner"]:IsA("Model"), "received instance is not of the Model class!")
			else
				value["ProjectileOwner"] = nil
			end
			if reliable_events[15][1] then
				for _, cb in reliable_events[15] do
					task.spawn(cb, value)
				end
			else
				table.insert(reliable_event_queue[15], value)
				if #reliable_event_queue[15] > 64 then
					warn(`[ZAP] {#reliable_event_queue[15]} events in queue for SpawnUniATKs. Did you forget to attach a listener?`)
				end
			end
		elseif id == 16 then
			local value
			value = {}
			local len_13 = buffer.readu16(incoming_buff, read(2))
			value["SlotId"] = buffer.readstring(incoming_buff, read(len_13), len_13)
			value["CooldownTime"] = buffer.readu8(incoming_buff, read(1))
			if reliable_events[16][1] then
				for _, cb in reliable_events[16] do
					task.spawn(cb, value)
				end
			else
				table.insert(reliable_event_queue[16], value)
				if #reliable_event_queue[16] > 64 then
					warn(`[ZAP] {#reliable_event_queue[16]} events in queue for RequestToCastItem_Response. Did you forget to attach a listener?`)
				end
			end
		elseif id == 17 then
			local value
			value = {}
			local len_14 = buffer.readu16(incoming_buff, read(2))
			value["SceneId"] = buffer.readstring(incoming_buff, read(len_14), len_14)
			if buffer.readu8(incoming_buff, read(1)) == 1 then
				incoming_ipos = incoming_ipos + 1
				value["Caster"] = incoming_inst[incoming_ipos]
				assert(value["Caster"] == nil or value["Caster"]:IsA("Model"), "received instance is not of the Model class!")
			else
				value["Caster"] = nil
			end
			local len_15 = buffer.readu16(incoming_buff, read(2))
			value["ProjectileUniqueId"] = buffer.readstring(incoming_buff, read(len_15), len_15)
			if reliable_events[17][1] then
				for _, cb in reliable_events[17] do
					task.spawn(cb, value)
				end
			else
				table.insert(reliable_event_queue[17], value)
				if #reliable_event_queue[17] > 64 then
					warn(`[ZAP] {#reliable_event_queue[17]} events in queue for PlayClassSPScene. Did you forget to attach a listener?`)
				end
			end
		elseif id == 18 then
			local value
			if reliable_events[18][1] then
				for _, cb in reliable_events[18] do
					task.spawn(cb, value)
				end
			else
				reliable_event_queue[18] += 1
				if reliable_event_queue[18] > 16 then
					warn(`[ZAP] {reliable_event_queue[18]} events in queue for FaceToTargetingEnemy. Did you forget to attach a listener?`)
				end
			end
		elseif id == 19 then
			local value
			value = {}
			value["Products"] = {}
			local len_16 = buffer.readu16(incoming_buff, read(2))
			for i_1 = 1, len_16 do
				local val_1
				val_1 = {}
				local len_17 = buffer.readu16(incoming_buff, read(2))
				val_1["ProductId"] = buffer.readstring(incoming_buff, read(len_17), len_17)
				local len_18 = buffer.readu16(incoming_buff, read(2))
				val_1["ProductUniqueId"] = buffer.readstring(incoming_buff, read(len_18), len_18)
				value["Products"][i_1] = val_1
			end
			if reliable_events[19][1] then
				for _, cb in reliable_events[19] do
					task.spawn(cb, value)
				end
			else
				table.insert(reliable_event_queue[19], value)
				if #reliable_event_queue[19] > 64 then
					warn(`[ZAP] {#reliable_event_queue[19]} events in queue for OpenMarketUI. Did you forget to attach a listener?`)
				end
			end
		elseif id == 20 then
			local value
			value = {}
			local len_19 = buffer.readu16(incoming_buff, read(2))
			value["AnimId"] = buffer.readstring(incoming_buff, read(len_19), len_19)
			if reliable_events[20][1] then
				for _, cb in reliable_events[20] do
					task.spawn(cb, value)
				end
			else
				table.insert(reliable_event_queue[20], value)
				if #reliable_event_queue[20] > 64 then
					warn(`[ZAP] {#reliable_event_queue[20]} events in queue for PlayAnimation. Did you forget to attach a listener?`)
				end
			end
		elseif id == 21 then
			local value
			value = {}
			if buffer.readu8(incoming_buff, read(1)) == 1 then
				incoming_ipos = incoming_ipos + 1
				value["Entity"] = incoming_inst[incoming_ipos]
				assert(value["Entity"] == nil or value["Entity"]:IsA("Model"), "received instance is not of the Model class!")
			else
				value["Entity"] = nil
			end
			local len_20 = buffer.readu16(incoming_buff, read(2))
			value["AnimId"] = buffer.readstring(incoming_buff, read(len_20), len_20)
			if reliable_events[21][1] then
				for _, cb in reliable_events[21] do
					task.spawn(cb, value)
				end
			else
				table.insert(reliable_event_queue[21], value)
				if #reliable_event_queue[21] > 64 then
					warn(`[ZAP] {#reliable_event_queue[21]} events in queue for PlayAnimForEnemy. Did you forget to attach a listener?`)
				end
			end
		elseif id == 22 then
			local value
			if reliable_events[22][1] then
				for _, cb in reliable_events[22] do
					task.spawn(cb, value)
				end
			else
				reliable_event_queue[22] += 1
				if reliable_event_queue[22] > 16 then
					warn(`[ZAP] {reliable_event_queue[22]} events in queue for OpenCraftingUI. Did you forget to attach a listener?`)
				end
			end
		elseif id == 23 then
			local value
			value = {}
			if buffer.readu8(incoming_buff, read(1)) == 1 then
				incoming_ipos = incoming_ipos + 1
				value["Entity"] = incoming_inst[incoming_ipos]
				assert(value["Entity"] == nil or value["Entity"]:IsA("Model"), "received instance is not of the Model class!")
			else
				value["Entity"] = nil
			end
			value["Duration"] = buffer.readu8(incoming_buff, read(1))
			if reliable_events[23][1] then
				for _, cb in reliable_events[23] do
					task.spawn(cb, value)
				end
			else
				table.insert(reliable_event_queue[23], value)
				if #reliable_event_queue[23] > 64 then
					warn(`[ZAP] {#reliable_event_queue[23]} events in queue for ApplyHighlightForEntity. Did you forget to attach a listener?`)
				end
			end
		elseif id == 24 then
			local value
			if reliable_events[24][1] then
				for _, cb in reliable_events[24] do
					task.spawn(cb, value)
				end
			else
				reliable_event_queue[24] += 1
				if reliable_event_queue[24] > 16 then
					warn(`[ZAP] {reliable_event_queue[24]} events in queue for Spell_ExtraJumpSupport. Did you forget to attach a listener?`)
				end
			end
		elseif id == 25 then
			local value
			value = {}
			if buffer.readu8(incoming_buff, read(1)) == 1 then
				incoming_ipos = incoming_ipos + 1
				value["Entity"] = incoming_inst[incoming_ipos]
				assert(value["Entity"] == nil or value["Entity"]:IsA("Model"), "received instance is not of the Model class!")
			else
				value["Entity"] = nil
			end
			value["ResonateNum"] = buffer.readu8(incoming_buff, read(1))
			if reliable_events[25][1] then
				for _, cb in reliable_events[25] do
					task.spawn(cb, value)
				end
			else
				table.insert(reliable_event_queue[25], value)
				if #reliable_event_queue[25] > 64 then
					warn(`[ZAP] {#reliable_event_queue[25]} events in queue for CLASS_RollNewResonateNumForEnemy. Did you forget to attach a listener?`)
				end
			end
		elseif id == 26 then
			local value
			value = {}
			value["ResonateNum"] = buffer.readu8(incoming_buff, read(1))
			if reliable_events[26][1] then
				for _, cb in reliable_events[26] do
					task.spawn(cb, value)
				end
			else
				table.insert(reliable_event_queue[26], value)
				if #reliable_event_queue[26] > 64 then
					warn(`[ZAP] {#reliable_event_queue[26]} events in queue for CLASS_RollNewResonateNumForPlayer. Did you forget to attach a listener?`)
				end
			end
		elseif id == 27 then
			local value, value2
			if buffer.readu8(incoming_buff, read(1)) == 1 then
				incoming_ipos = incoming_ipos + 1
				value = incoming_inst[incoming_ipos]
				assert(value == nil or value:IsA("Sound"), "received instance is not of the Sound class!")
			else
				value = nil
			end
			if buffer.readu8(incoming_buff, read(1)) == 1 then
				value2 = buffer.readu8(incoming_buff, read(1))
			else
				value2 = nil
			end
			if reliable_events[27][1] then
				for _, cb in reliable_events[27] do
					task.spawn(cb, value, value2)
				end
			else
				table.insert(reliable_event_queue[27], { value, value2 })
				if #reliable_event_queue[27] > 64 then
					warn(`[ZAP] {#reliable_event_queue[27]} events in queue for PlaySound. Did you forget to attach a listener?`)
				end
			end
		elseif id == 28 then
			local value
			value = {}
			value["Objs"] = {}
			local len_21 = buffer.readu16(incoming_buff, read(2))
			for i_2 = 1, len_21 do
				local val_2
				if buffer.readu8(incoming_buff, read(1)) == 1 then
					incoming_ipos = incoming_ipos + 1
					val_2 = incoming_inst[incoming_ipos]
					assert(val_2 == nil or val_2:IsA("BasePart"), "received instance is not of the BasePart class!")
				else
					val_2 = nil
				end
				value["Objs"][i_2] = val_2
			end
			value["Goal"] = {}
			for _ = 1, buffer.readu16(incoming_buff, read(2)) do
				local key_1
				local val_3
				local len_22 = buffer.readu16(incoming_buff, read(2))
				key_1 = buffer.readstring(incoming_buff, read(len_22), len_22)
				if buffer.readu8(incoming_buff, read(1)) == 1 then
					incoming_ipos = incoming_ipos + 1
					val_3 = incoming_inst[incoming_ipos]
				else
					val_3 = nil
				end
				value["Goal"][key_1] = val_3
			end
			local len_23 = buffer.readu16(incoming_buff, read(2))
			value["TweenInfo"] = buffer.readstring(incoming_buff, read(len_23), len_23)
			value["UseHarukaTween"] = buffer.readu8(incoming_buff, read(1)) == 1
			if reliable_events[28][1] then
				for _, cb in reliable_events[28] do
					task.spawn(cb, value)
				end
			else
				table.insert(reliable_event_queue[28], value)
				if #reliable_event_queue[28] > 64 then
					warn(`[ZAP] {#reliable_event_queue[28]} events in queue for PlayClientTween. Did you forget to attach a listener?`)
				end
			end
		elseif id == 29 then
			local value
			if buffer.readu8(incoming_buff, read(1)) == 1 then
				incoming_ipos = incoming_ipos + 1
				value = incoming_inst[incoming_ipos]
				assert(value == nil or value:IsA("BasePart"), "received instance is not of the BasePart class!")
			else
				value = nil
			end
			if reliable_events[29][1] then
				for _, cb in reliable_events[29] do
					task.spawn(cb, value)
				end
			else
				table.insert(reliable_event_queue[29], value)
				if #reliable_event_queue[29] > 64 then
					warn(`[ZAP] {#reliable_event_queue[29]} events in queue for PlayCameraCutscene. Did you forget to attach a listener?`)
				end
			end
		elseif id == 30 then
			local value
			local len_24 = buffer.readu16(incoming_buff, read(2))
			value = buffer.readstring(incoming_buff, read(len_24), len_24)
			if reliable_events[30][1] then
				for _, cb in reliable_events[30] do
					task.spawn(cb, value)
				end
			else
				table.insert(reliable_event_queue[30], value)
				if #reliable_event_queue[30] > 64 then
					warn(`[ZAP] {#reliable_event_queue[30]} events in queue for CreateNotify. Did you forget to attach a listener?`)
				end
			end
		elseif id == 31 then
			local value
			local len_25 = buffer.readu16(incoming_buff, read(2))
			value = buffer.readstring(incoming_buff, read(len_25), len_25)
			if reliable_events[31][1] then
				for _, cb in reliable_events[31] do
					task.spawn(cb, value)
				end
			else
				table.insert(reliable_event_queue[31], value)
				if #reliable_event_queue[31] > 64 then
					warn(`[ZAP] {#reliable_event_queue[31]} events in queue for CreateSystemNotify. Did you forget to attach a listener?`)
				end
			end
		elseif id == 32 then
			local value, value2
			local len_26 = buffer.readu16(incoming_buff, read(2))
			value = buffer.readstring(incoming_buff, read(len_26), len_26)
			value2 = buffer.readu8(incoming_buff, read(1)) == 1
			if reliable_events[32][1] then
				for _, cb in reliable_events[32] do
					task.spawn(cb, value, value2)
				end
			else
				table.insert(reliable_event_queue[32], { value, value2 })
				if #reliable_event_queue[32] > 64 then
					warn(`[ZAP] {#reliable_event_queue[32]} events in queue for CreateCenterNotify. Did you forget to attach a listener?`)
				end
			end
		elseif id == 33 then
			local value, value2
			local len_27 = buffer.readu16(incoming_buff, read(2))
			value = buffer.readstring(incoming_buff, read(len_27), len_27)
			value2 = buffer.readu8(incoming_buff, read(1))
			if reliable_events[33][1] then
				for _, cb in reliable_events[33] do
					task.spawn(cb, value, value2)
				end
			else
				table.insert(reliable_event_queue[33], { value, value2 })
				if #reliable_event_queue[33] > 64 then
					warn(`[ZAP] {#reliable_event_queue[33]} events in queue for CreateFSNotify. Did you forget to attach a listener?`)
				end
			end
		elseif id == 34 then
			local value
			value = buffer.readu8(incoming_buff, read(1))
			if reliable_events[34][1] then
				for _, cb in reliable_events[34] do
					task.spawn(cb, value)
				end
			else
				table.insert(reliable_event_queue[34], value)
				if #reliable_event_queue[34] > 64 then
					warn(`[ZAP] {#reliable_event_queue[34]} events in queue for CreateCircularFade. Did you forget to attach a listener?`)
				end
			end
		else
			error("Unknown event id")
		end
	end
end)
table.freeze(polling_queues_reliable)
table.freeze(polling_queues_unreliable)

local returns = {
	SendEvents = SendEvents,
	RequestToChooseWhichSlotToPlay = {
		Fire = function(Value: ({
			["SlotId"]: ((string)?),
		}))
			alloc(1)
			buffer.writeu8(outgoing_buff, outgoing_apos, 0)
			if Value["SlotId"] == nil then
				alloc(1)
				buffer.writeu8(outgoing_buff, outgoing_apos, 0)
			else
				alloc(1)
				buffer.writeu8(outgoing_buff, outgoing_apos, 1)
				local len_28 = #Value["SlotId"]
				alloc(2)
				buffer.writeu16(outgoing_buff, outgoing_apos, len_28)
				alloc(len_28)
				buffer.writestring(outgoing_buff, outgoing_apos, Value["SlotId"], len_28)
			end
		end,
	},
	RequestToEraseSaveFile = {
		Fire = function(Value: ({
			["SlotId"]: ((string)?),
		}))
			alloc(1)
			buffer.writeu8(outgoing_buff, outgoing_apos, 1)
			if Value["SlotId"] == nil then
				alloc(1)
				buffer.writeu8(outgoing_buff, outgoing_apos, 0)
			else
				alloc(1)
				buffer.writeu8(outgoing_buff, outgoing_apos, 1)
				local len_29 = #Value["SlotId"]
				alloc(2)
				buffer.writeu16(outgoing_buff, outgoing_apos, len_29)
				alloc(len_29)
				buffer.writestring(outgoing_buff, outgoing_apos, Value["SlotId"], len_29)
			end
		end,
	},
	RequestToEndNPCChat = {
		Fire = function(Value: ({
			["NPCId"]: ((string)?),
			["SeriesId"]: ((string)?),
		}))
			alloc(1)
			buffer.writeu8(outgoing_buff, outgoing_apos, 2)
			if Value["NPCId"] == nil then
				alloc(1)
				buffer.writeu8(outgoing_buff, outgoing_apos, 0)
			else
				alloc(1)
				buffer.writeu8(outgoing_buff, outgoing_apos, 1)
				local len_30 = #Value["NPCId"]
				alloc(2)
				buffer.writeu16(outgoing_buff, outgoing_apos, len_30)
				alloc(len_30)
				buffer.writestring(outgoing_buff, outgoing_apos, Value["NPCId"], len_30)
			end
			if Value["SeriesId"] == nil then
				alloc(1)
				buffer.writeu8(outgoing_buff, outgoing_apos, 0)
			else
				alloc(1)
				buffer.writeu8(outgoing_buff, outgoing_apos, 1)
				local len_31 = #Value["SeriesId"]
				alloc(2)
				buffer.writeu16(outgoing_buff, outgoing_apos, len_31)
				alloc(len_31)
				buffer.writestring(outgoing_buff, outgoing_apos, Value["SeriesId"], len_31)
			end
		end,
	},
	RequestToDMGEnemy = {
		Fire = function(Value: ({
			["OrbUniqueId"]: ((string)?),
		}))
			alloc(1)
			buffer.writeu8(outgoing_buff, outgoing_apos, 3)
			if Value["OrbUniqueId"] == nil then
				alloc(1)
				buffer.writeu8(outgoing_buff, outgoing_apos, 0)
			else
				alloc(1)
				buffer.writeu8(outgoing_buff, outgoing_apos, 1)
				local len_32 = #Value["OrbUniqueId"]
				alloc(2)
				buffer.writeu16(outgoing_buff, outgoing_apos, len_32)
				alloc(len_32)
				buffer.writestring(outgoing_buff, outgoing_apos, Value["OrbUniqueId"], len_32)
			end
		end,
	},
	RequestToDMGPlayer = {
		Fire = function(Value: ({
			["ProjectileUniqueId"]: ((string)?),
			["IsPierce"]: ((boolean)?),
			["DoTStatus"]: ((string)?),
		}))
			alloc(1)
			buffer.writeu8(outgoing_buff, outgoing_apos, 4)
			if Value["ProjectileUniqueId"] == nil then
				alloc(1)
				buffer.writeu8(outgoing_buff, outgoing_apos, 0)
			else
				alloc(1)
				buffer.writeu8(outgoing_buff, outgoing_apos, 1)
				local len_33 = #Value["ProjectileUniqueId"]
				alloc(2)
				buffer.writeu16(outgoing_buff, outgoing_apos, len_33)
				alloc(len_33)
				buffer.writestring(outgoing_buff, outgoing_apos, Value["ProjectileUniqueId"], len_33)
			end
			if Value["IsPierce"] == nil then
				alloc(1)
				buffer.writeu8(outgoing_buff, outgoing_apos, 0)
			else
				alloc(1)
				buffer.writeu8(outgoing_buff, outgoing_apos, 1)
				alloc(1)
				buffer.writeu8(outgoing_buff, outgoing_apos, Value["IsPierce"] and 1 or 0)
			end
			if Value["DoTStatus"] == nil then
				alloc(1)
				buffer.writeu8(outgoing_buff, outgoing_apos, 0)
			else
				alloc(1)
				buffer.writeu8(outgoing_buff, outgoing_apos, 1)
				local len_34 = #Value["DoTStatus"]
				alloc(2)
				buffer.writeu16(outgoing_buff, outgoing_apos, len_34)
				alloc(len_34)
				buffer.writestring(outgoing_buff, outgoing_apos, Value["DoTStatus"], len_34)
			end
		end,
	},
	RequestToCastItem_Request = {
		Fire = function(Value: ({
			["SlotId"]: ((string)?),
		}))
			alloc(1)
			buffer.writeu8(outgoing_buff, outgoing_apos, 5)
			if Value["SlotId"] == nil then
				alloc(1)
				buffer.writeu8(outgoing_buff, outgoing_apos, 0)
			else
				alloc(1)
				buffer.writeu8(outgoing_buff, outgoing_apos, 1)
				local len_35 = #Value["SlotId"]
				alloc(2)
				buffer.writeu16(outgoing_buff, outgoing_apos, len_35)
				alloc(len_35)
				buffer.writestring(outgoing_buff, outgoing_apos, Value["SlotId"], len_35)
			end
		end,
	},
	RequestToCastClassUlt = {
		Fire = function()
			alloc(1)
			buffer.writeu8(outgoing_buff, outgoing_apos, 6)
		end,
	},
	RequestToPickupItemDrops = {
		Fire = function(Value: ({
			["ItemUniqueId"]: ((string)?),
		}))
			alloc(1)
			buffer.writeu8(outgoing_buff, outgoing_apos, 7)
			if Value["ItemUniqueId"] == nil then
				alloc(1)
				buffer.writeu8(outgoing_buff, outgoing_apos, 0)
			else
				alloc(1)
				buffer.writeu8(outgoing_buff, outgoing_apos, 1)
				local len_36 = #Value["ItemUniqueId"]
				alloc(2)
				buffer.writeu16(outgoing_buff, outgoing_apos, len_36)
				alloc(len_36)
				buffer.writestring(outgoing_buff, outgoing_apos, Value["ItemUniqueId"], len_36)
			end
		end,
	},
	RequestToEquipItem = {
		Fire = function(Value: ({
			["ItemId"]: ((string)?),
			["ItemUniqueId"]: ((string)?),
		}))
			alloc(1)
			buffer.writeu8(outgoing_buff, outgoing_apos, 8)
			if Value["ItemId"] == nil then
				alloc(1)
				buffer.writeu8(outgoing_buff, outgoing_apos, 0)
			else
				alloc(1)
				buffer.writeu8(outgoing_buff, outgoing_apos, 1)
				local len_37 = #Value["ItemId"]
				alloc(2)
				buffer.writeu16(outgoing_buff, outgoing_apos, len_37)
				alloc(len_37)
				buffer.writestring(outgoing_buff, outgoing_apos, Value["ItemId"], len_37)
			end
			if Value["ItemUniqueId"] == nil then
				alloc(1)
				buffer.writeu8(outgoing_buff, outgoing_apos, 0)
			else
				alloc(1)
				buffer.writeu8(outgoing_buff, outgoing_apos, 1)
				local len_38 = #Value["ItemUniqueId"]
				alloc(2)
				buffer.writeu16(outgoing_buff, outgoing_apos, len_38)
				alloc(len_38)
				buffer.writestring(outgoing_buff, outgoing_apos, Value["ItemUniqueId"], len_38)
			end
		end,
	},
	RequestToPurchaseItem = {
		Fire = function(Value: ({
			["ProductId"]: ((string)?),
			["ProductUniqueId"]: ((string)?),
			["PurchaseAmount"]: ((number)?),
		}))
			alloc(1)
			buffer.writeu8(outgoing_buff, outgoing_apos, 9)
			if Value["ProductId"] == nil then
				alloc(1)
				buffer.writeu8(outgoing_buff, outgoing_apos, 0)
			else
				alloc(1)
				buffer.writeu8(outgoing_buff, outgoing_apos, 1)
				local len_39 = #Value["ProductId"]
				alloc(2)
				buffer.writeu16(outgoing_buff, outgoing_apos, len_39)
				alloc(len_39)
				buffer.writestring(outgoing_buff, outgoing_apos, Value["ProductId"], len_39)
			end
			if Value["ProductUniqueId"] == nil then
				alloc(1)
				buffer.writeu8(outgoing_buff, outgoing_apos, 0)
			else
				alloc(1)
				buffer.writeu8(outgoing_buff, outgoing_apos, 1)
				local len_40 = #Value["ProductUniqueId"]
				alloc(2)
				buffer.writeu16(outgoing_buff, outgoing_apos, len_40)
				alloc(len_40)
				buffer.writestring(outgoing_buff, outgoing_apos, Value["ProductUniqueId"], len_40)
			end
			if Value["PurchaseAmount"] == nil then
				alloc(1)
				buffer.writeu8(outgoing_buff, outgoing_apos, 0)
			else
				alloc(1)
				buffer.writeu8(outgoing_buff, outgoing_apos, 1)
				alloc(2)
				buffer.writeu16(outgoing_buff, outgoing_apos, Value["PurchaseAmount"])
			end
		end,
	},
	RequestToSellItem = {
		Fire = function(Value: ({
			["ItemId"]: ((string)?),
			["ItemUniqueId"]: ((string)?),
			["SellAmount"]: ((number)?),
		}))
			alloc(1)
			buffer.writeu8(outgoing_buff, outgoing_apos, 10)
			if Value["ItemId"] == nil then
				alloc(1)
				buffer.writeu8(outgoing_buff, outgoing_apos, 0)
			else
				alloc(1)
				buffer.writeu8(outgoing_buff, outgoing_apos, 1)
				local len_41 = #Value["ItemId"]
				alloc(2)
				buffer.writeu16(outgoing_buff, outgoing_apos, len_41)
				alloc(len_41)
				buffer.writestring(outgoing_buff, outgoing_apos, Value["ItemId"], len_41)
			end
			if Value["ItemUniqueId"] == nil then
				alloc(1)
				buffer.writeu8(outgoing_buff, outgoing_apos, 0)
			else
				alloc(1)
				buffer.writeu8(outgoing_buff, outgoing_apos, 1)
				local len_42 = #Value["ItemUniqueId"]
				alloc(2)
				buffer.writeu16(outgoing_buff, outgoing_apos, len_42)
				alloc(len_42)
				buffer.writestring(outgoing_buff, outgoing_apos, Value["ItemUniqueId"], len_42)
			end
			if Value["SellAmount"] == nil then
				alloc(1)
				buffer.writeu8(outgoing_buff, outgoing_apos, 0)
			else
				alloc(1)
				buffer.writeu8(outgoing_buff, outgoing_apos, 1)
				alloc(2)
				buffer.writeu16(outgoing_buff, outgoing_apos, Value["SellAmount"])
			end
		end,
	},
	RequestToAddPoint = {
		Fire = function(Value: ({
			["PointId"]: ((string)?),
			["PointAmount"]: ((number)?),
		}))
			alloc(1)
			buffer.writeu8(outgoing_buff, outgoing_apos, 11)
			if Value["PointId"] == nil then
				alloc(1)
				buffer.writeu8(outgoing_buff, outgoing_apos, 0)
			else
				alloc(1)
				buffer.writeu8(outgoing_buff, outgoing_apos, 1)
				local len_43 = #Value["PointId"]
				alloc(2)
				buffer.writeu16(outgoing_buff, outgoing_apos, len_43)
				alloc(len_43)
				buffer.writestring(outgoing_buff, outgoing_apos, Value["PointId"], len_43)
			end
			if Value["PointAmount"] == nil then
				alloc(1)
				buffer.writeu8(outgoing_buff, outgoing_apos, 0)
			else
				alloc(1)
				buffer.writeu8(outgoing_buff, outgoing_apos, 1)
				alloc(2)
				buffer.writei16(outgoing_buff, outgoing_apos, Value["PointAmount"])
			end
		end,
	},
	RequestToDMGEnemyBySkillProjectile = {
		Fire = function(Value: ({
			["ProjectileUniqueId"]: ((string)?),
			["EnemyEntity"]: ((Model)?),
		}))
			alloc(1)
			buffer.writeu8(outgoing_buff, outgoing_apos, 12)
			if Value["ProjectileUniqueId"] == nil then
				alloc(1)
				buffer.writeu8(outgoing_buff, outgoing_apos, 0)
			else
				alloc(1)
				buffer.writeu8(outgoing_buff, outgoing_apos, 1)
				local len_44 = #Value["ProjectileUniqueId"]
				alloc(2)
				buffer.writeu16(outgoing_buff, outgoing_apos, len_44)
				alloc(len_44)
				buffer.writestring(outgoing_buff, outgoing_apos, Value["ProjectileUniqueId"], len_44)
			end
			if Value["EnemyEntity"] == nil then
				alloc(1)
				buffer.writeu8(outgoing_buff, outgoing_apos, 0)
			else
				alloc(1)
				buffer.writeu8(outgoing_buff, outgoing_apos, 1)
				assert(Value["EnemyEntity"]:IsA("Model"), "received instance is not of the Model class!")
				table.insert(outgoing_inst, Value["EnemyEntity"])
			end
		end,
	},
	RequestToStartCombat = {
		Fire = function(Value: ({
			["Entity"]: ((Model)?),
		}))
			alloc(1)
			buffer.writeu8(outgoing_buff, outgoing_apos, 13)
			if Value["Entity"] == nil then
				alloc(1)
				buffer.writeu8(outgoing_buff, outgoing_apos, 0)
			else
				alloc(1)
				buffer.writeu8(outgoing_buff, outgoing_apos, 1)
				assert(Value["Entity"]:IsA("Model"), "received instance is not of the Model class!")
				table.insert(outgoing_inst, Value["Entity"])
			end
		end,
	},
	RequestToResetPoint = {
		Fire = function()
			alloc(1)
			buffer.writeu8(outgoing_buff, outgoing_apos, 14)
		end,
	},
	RequestToSellAllItems = {
		Fire = function(Value: ({
			["ItemId"]: ((string)?),
		}))
			alloc(1)
			buffer.writeu8(outgoing_buff, outgoing_apos, 15)
			if Value["ItemId"] == nil then
				alloc(1)
				buffer.writeu8(outgoing_buff, outgoing_apos, 0)
			else
				alloc(1)
				buffer.writeu8(outgoing_buff, outgoing_apos, 1)
				local len_45 = #Value["ItemId"]
				alloc(2)
				buffer.writeu16(outgoing_buff, outgoing_apos, len_45)
				alloc(len_45)
				buffer.writestring(outgoing_buff, outgoing_apos, Value["ItemId"], len_45)
			end
		end,
	},
	RequestToCraftItem = {
		Fire = function(Value: ({
			["ItemId"]: ((string)?),
			["ItemUniqueId"]: ((string)?),
		}))
			alloc(1)
			buffer.writeu8(outgoing_buff, outgoing_apos, 16)
			if Value["ItemId"] == nil then
				alloc(1)
				buffer.writeu8(outgoing_buff, outgoing_apos, 0)
			else
				alloc(1)
				buffer.writeu8(outgoing_buff, outgoing_apos, 1)
				local len_46 = #Value["ItemId"]
				alloc(2)
				buffer.writeu16(outgoing_buff, outgoing_apos, len_46)
				alloc(len_46)
				buffer.writestring(outgoing_buff, outgoing_apos, Value["ItemId"], len_46)
			end
			if Value["ItemUniqueId"] == nil then
				alloc(1)
				buffer.writeu8(outgoing_buff, outgoing_apos, 0)
			else
				alloc(1)
				buffer.writeu8(outgoing_buff, outgoing_apos, 1)
				local len_47 = #Value["ItemUniqueId"]
				alloc(2)
				buffer.writeu16(outgoing_buff, outgoing_apos, len_47)
				alloc(len_47)
				buffer.writestring(outgoing_buff, outgoing_apos, Value["ItemUniqueId"], len_47)
			end
		end,
	},
	RequestToSaveSettings = {
		Fire = function(Value: ({
			["SettingId"]: ((string)?),
			["SettingValue"]: ((unknown)),
		}))
			alloc(1)
			buffer.writeu8(outgoing_buff, outgoing_apos, 17)
			if Value["SettingId"] == nil then
				alloc(1)
				buffer.writeu8(outgoing_buff, outgoing_apos, 0)
			else
				alloc(1)
				buffer.writeu8(outgoing_buff, outgoing_apos, 1)
				local len_48 = #Value["SettingId"]
				alloc(2)
				buffer.writeu16(outgoing_buff, outgoing_apos, len_48)
				alloc(len_48)
				buffer.writestring(outgoing_buff, outgoing_apos, Value["SettingId"], len_48)
			end
			if Value["SettingValue"] == nil then
				alloc(1)
				buffer.writeu8(outgoing_buff, outgoing_apos, 0)
			else
				alloc(1)
				buffer.writeu8(outgoing_buff, outgoing_apos, 1)
				table.insert(outgoing_inst, Value["SettingValue"])
			end
		end,
	},
	RequestToForceReset = {
		Fire = function()
			alloc(1)
			buffer.writeu8(outgoing_buff, outgoing_apos, 18)
		end,
	},
	RequestToRejoinGame = {
		Fire = function()
			alloc(1)
			buffer.writeu8(outgoing_buff, outgoing_apos, 19)
		end,
	},
	ApplyFoundStatusToEnemy = {
		On = function(Callback: (Value: ({
			["Enemy"]: ((Model)?),
		})) -> ())
			table.insert(reliable_events[0], Callback)
			for _, value in reliable_event_queue[0] do
				task.spawn(Callback, value)
			end
			reliable_event_queue[0] = {}
			return function()
				table.remove(reliable_events[0], table.find(reliable_events[0], Callback))
			end
		end,
	},
	PlaceOrb = {
		On = function(Callback: (Value: ({
			["OrbId"]: (string),
			["OrbUniqueId"]: (string),
			["OrbDuration"]: (number),
			["OrbLocationPos"]: (Vector3),
		})) -> ())
			table.insert(reliable_events[1], Callback)
			for _, value in reliable_event_queue[1] do
				task.spawn(Callback, value)
			end
			reliable_event_queue[1] = {}
			return function()
				table.remove(reliable_events[1], table.find(reliable_events[1], Callback))
			end
		end,
	},
	EnterNPCChat = {
		On = function(Callback: (Value: ({
			["NPCId"]: (string),
		})) -> ())
			table.insert(reliable_events[2], Callback)
			for _, value in reliable_event_queue[2] do
				task.spawn(Callback, value)
			end
			reliable_event_queue[2] = {}
			return function()
				table.remove(reliable_events[2], table.find(reliable_events[2], Callback))
			end
		end,
	},
	OpenCastTimeBar = {
		On = function(Callback: (Value: ({
			["Duration"]: (number),
		})) -> ())
			table.insert(reliable_events[3], Callback)
			for _, value in reliable_event_queue[3] do
				task.spawn(Callback, value)
			end
			reliable_event_queue[3] = {}
			return function()
				table.remove(reliable_events[3], table.find(reliable_events[3], Callback))
			end
		end,
	},
	PlayGotHitScreenFX = {
		On = function(Callback: () -> ())
			table.insert(reliable_events[4], Callback)
			for _ = 1, reliable_event_queue[4] do
				task.spawn(Callback)
			end
			reliable_event_queue[4] = 0
			return function()
				table.remove(reliable_events[4], table.find(reliable_events[4], Callback))
			end
		end,
	},
	PlayStatPoweredScreenFX = {
		On = function(Callback: () -> ())
			table.insert(reliable_events[5], Callback)
			for _ = 1, reliable_event_queue[5] do
				task.spawn(Callback)
			end
			reliable_event_queue[5] = 0
			return function()
				table.remove(reliable_events[5], table.find(reliable_events[5], Callback))
			end
		end,
	},
	PlayHealScreenFX = {
		On = function(Callback: () -> ())
			table.insert(reliable_events[6], Callback)
			for _ = 1, reliable_event_queue[6] do
				task.spawn(Callback)
			end
			reliable_event_queue[6] = 0
			return function()
				table.remove(reliable_events[6], table.find(reliable_events[6], Callback))
			end
		end,
	},
	PlayItemDropFX = {
		On = function(Callback: (Value: ({
			["ItemId"]: (string),
			["Amount"]: (number),
		})) -> ())
			table.insert(reliable_events[7], Callback)
			for _, value in reliable_event_queue[7] do
				task.spawn(Callback, value)
			end
			reliable_event_queue[7] = {}
			return function()
				table.remove(reliable_events[7], table.find(reliable_events[7], Callback))
			end
		end,
	},
	OpenEXPAwardBar = {
		On = function(Callback: (Value: ({
			["EXPGained"]: (number),
		})) -> ())
			table.insert(reliable_events[8], Callback)
			for _, value in reliable_event_queue[8] do
				task.spawn(Callback, value)
			end
			reliable_event_queue[8] = {}
			return function()
				table.remove(reliable_events[8], table.find(reliable_events[8], Callback))
			end
		end,
	},
	ShowDMGNumber = {
		On = function(Callback: (Value: ({
			["DMGTarget"]: ((Model)?),
			["DMGNumber"]: (string),
			["DMGColor"]: (Color3),
		})) -> ())
			table.insert(reliable_events[9], Callback)
			for _, value in reliable_event_queue[9] do
				task.spawn(Callback, value)
			end
			reliable_event_queue[9] = {}
			return function()
				table.remove(reliable_events[9], table.find(reliable_events[9], Callback))
			end
		end,
	},
	ShowText = {
		On = function(Callback: (Value: ({
			["Target"]: ((Model)?),
			["Text"]: (string),
			["TextColor"]: (Color3),
		})) -> ())
			table.insert(reliable_events[10], Callback)
			for _, value in reliable_event_queue[10] do
				task.spawn(Callback, value)
			end
			reliable_event_queue[10] = {}
			return function()
				table.remove(reliable_events[10], table.find(reliable_events[10], Callback))
			end
		end,
	},
	PlayHitStatusVFX = {
		On = function(Callback: (Value: ({
			["Target"]: ((Model)?),
		})) -> ())
			table.insert(reliable_events[11], Callback)
			for _, value in reliable_event_queue[11] do
				task.spawn(Callback, value)
			end
			reliable_event_queue[11] = {}
			return function()
				table.remove(reliable_events[11], table.find(reliable_events[11], Callback))
			end
		end,
	},
	SpawnItemDrop = {
		On = function(Callback: (Value: ({
			["ItemUniqueId"]: (string),
			["ItemId"]: (string),
			["ItemAmount"]: (number),
			["ItemDropLocationPos"]: (Vector3),
		})) -> ())
			table.insert(reliable_events[12], Callback)
			for _, value in reliable_event_queue[12] do
				task.spawn(Callback, value)
			end
			reliable_event_queue[12] = {}
			return function()
				table.remove(reliable_events[12], table.find(reliable_events[12], Callback))
			end
		end,
	},
	OpenChestClientTween = {
		On = function(Callback: (Value: ({
			["Chest"]: ((Model)?),
		})) -> ())
			table.insert(reliable_events[13], Callback)
			for _, value in reliable_event_queue[13] do
				task.spawn(Callback, value)
			end
			reliable_event_queue[13] = {}
			return function()
				table.remove(reliable_events[13], table.find(reliable_events[13], Callback))
			end
		end,
	},
	SpawnATKs = {
		On = function(Callback: (Value: ({
			["ProjectileId"]: (string),
			["ProjectileUniqueId"]: (string),
			["ProjectileCF"]: (CFrame),
			["ProjectileOwner"]: ((Model)?),
		})) -> ())
			table.insert(reliable_events[14], Callback)
			for _, value in reliable_event_queue[14] do
				task.spawn(Callback, value)
			end
			reliable_event_queue[14] = {}
			return function()
				table.remove(reliable_events[14], table.find(reliable_events[14], Callback))
			end
		end,
	},
	SpawnUniATKs = {
		On = function(Callback: (Value: ({
			["ProjectileId"]: (string),
			["ProjectileUniqueId"]: (string),
			["ProjectileCF"]: (CFrame),
			["ProjectileOwner"]: ((Model)?),
		})) -> ())
			table.insert(reliable_events[15], Callback)
			for _, value in reliable_event_queue[15] do
				task.spawn(Callback, value)
			end
			reliable_event_queue[15] = {}
			return function()
				table.remove(reliable_events[15], table.find(reliable_events[15], Callback))
			end
		end,
	},
	RequestToCastItem_Response = {
		On = function(Callback: (Value: ({
			["SlotId"]: (string),
			["CooldownTime"]: (number),
		})) -> ())
			table.insert(reliable_events[16], Callback)
			for _, value in reliable_event_queue[16] do
				task.spawn(Callback, value)
			end
			reliable_event_queue[16] = {}
			return function()
				table.remove(reliable_events[16], table.find(reliable_events[16], Callback))
			end
		end,
	},
	PlayClassSPScene = {
		On = function(Callback: (Value: ({
			["SceneId"]: (string),
			["Caster"]: ((Model)?),
			["ProjectileUniqueId"]: (string),
		})) -> ())
			table.insert(reliable_events[17], Callback)
			for _, value in reliable_event_queue[17] do
				task.spawn(Callback, value)
			end
			reliable_event_queue[17] = {}
			return function()
				table.remove(reliable_events[17], table.find(reliable_events[17], Callback))
			end
		end,
	},
	FaceToTargetingEnemy = {
		On = function(Callback: () -> ())
			table.insert(reliable_events[18], Callback)
			for _ = 1, reliable_event_queue[18] do
				task.spawn(Callback)
			end
			reliable_event_queue[18] = 0
			return function()
				table.remove(reliable_events[18], table.find(reliable_events[18], Callback))
			end
		end,
	},
	OpenMarketUI = {
		On = function(Callback: (Value: ({
			["Products"]: ({ ({
				["ProductId"]: (string),
				["ProductUniqueId"]: (string),
			}) }),
		})) -> ())
			table.insert(reliable_events[19], Callback)
			for _, value in reliable_event_queue[19] do
				task.spawn(Callback, value)
			end
			reliable_event_queue[19] = {}
			return function()
				table.remove(reliable_events[19], table.find(reliable_events[19], Callback))
			end
		end,
	},
	PlayAnimation = {
		On = function(Callback: (Value: ({
			["AnimId"]: (string),
		})) -> ())
			table.insert(reliable_events[20], Callback)
			for _, value in reliable_event_queue[20] do
				task.spawn(Callback, value)
			end
			reliable_event_queue[20] = {}
			return function()
				table.remove(reliable_events[20], table.find(reliable_events[20], Callback))
			end
		end,
	},
	PlayAnimForEnemy = {
		On = function(Callback: (Value: ({
			["Entity"]: ((Model)?),
			["AnimId"]: (string),
		})) -> ())
			table.insert(reliable_events[21], Callback)
			for _, value in reliable_event_queue[21] do
				task.spawn(Callback, value)
			end
			reliable_event_queue[21] = {}
			return function()
				table.remove(reliable_events[21], table.find(reliable_events[21], Callback))
			end
		end,
	},
	OpenCraftingUI = {
		On = function(Callback: () -> ())
			table.insert(reliable_events[22], Callback)
			for _ = 1, reliable_event_queue[22] do
				task.spawn(Callback)
			end
			reliable_event_queue[22] = 0
			return function()
				table.remove(reliable_events[22], table.find(reliable_events[22], Callback))
			end
		end,
	},
	ApplyHighlightForEntity = {
		On = function(Callback: (Value: ({
			["Entity"]: ((Model)?),
			["Duration"]: (number),
		})) -> ())
			table.insert(reliable_events[23], Callback)
			for _, value in reliable_event_queue[23] do
				task.spawn(Callback, value)
			end
			reliable_event_queue[23] = {}
			return function()
				table.remove(reliable_events[23], table.find(reliable_events[23], Callback))
			end
		end,
	},
	Spell_ExtraJumpSupport = {
		On = function(Callback: () -> ())
			table.insert(reliable_events[24], Callback)
			for _ = 1, reliable_event_queue[24] do
				task.spawn(Callback)
			end
			reliable_event_queue[24] = 0
			return function()
				table.remove(reliable_events[24], table.find(reliable_events[24], Callback))
			end
		end,
	},
	CLASS_RollNewResonateNumForEnemy = {
		On = function(Callback: (Value: ({
			["Entity"]: ((Model)?),
			["ResonateNum"]: (number),
		})) -> ())
			table.insert(reliable_events[25], Callback)
			for _, value in reliable_event_queue[25] do
				task.spawn(Callback, value)
			end
			reliable_event_queue[25] = {}
			return function()
				table.remove(reliable_events[25], table.find(reliable_events[25], Callback))
			end
		end,
	},
	CLASS_RollNewResonateNumForPlayer = {
		On = function(Callback: (Value: ({
			["ResonateNum"]: (number),
		})) -> ())
			table.insert(reliable_events[26], Callback)
			for _, value in reliable_event_queue[26] do
				task.spawn(Callback, value)
			end
			reliable_event_queue[26] = {}
			return function()
				table.remove(reliable_events[26], table.find(reliable_events[26], Callback))
			end
		end,
	},
	PlaySound = {
		On = function(Callback: (sound: ((Sound)?), timePos: ((number)?)) -> ())
			table.insert(reliable_events[27], Callback)
			for _, value in reliable_event_queue[27] do
				task.spawn(Callback, unpack(value))
			end
			reliable_event_queue[27] = {}
			return function()
				table.remove(reliable_events[27], table.find(reliable_events[27], Callback))
			end
		end,
	},
	PlayClientTween = {
		On = function(Callback: (Value: ({
			["Objs"]: ({ ((BasePart)?) }),
			["Goal"]: ({ [(string)]: ((unknown)) }),
			["TweenInfo"]: (string),
			["UseHarukaTween"]: (boolean),
		})) -> ())
			table.insert(reliable_events[28], Callback)
			for _, value in reliable_event_queue[28] do
				task.spawn(Callback, value)
			end
			reliable_event_queue[28] = {}
			return function()
				table.remove(reliable_events[28], table.find(reliable_events[28], Callback))
			end
		end,
	},
	PlayCameraCutscene = {
		On = function(Callback: (part: ((BasePart)?)) -> ())
			table.insert(reliable_events[29], Callback)
			for _, value in reliable_event_queue[29] do
				task.spawn(Callback, value)
			end
			reliable_event_queue[29] = {}
			return function()
				table.remove(reliable_events[29], table.find(reliable_events[29], Callback))
			end
		end,
	},
	CreateNotify = {
		On = function(Callback: (text: (string)) -> ())
			table.insert(reliable_events[30], Callback)
			for _, value in reliable_event_queue[30] do
				task.spawn(Callback, value)
			end
			reliable_event_queue[30] = {}
			return function()
				table.remove(reliable_events[30], table.find(reliable_events[30], Callback))
			end
		end,
	},
	CreateSystemNotify = {
		On = function(Callback: (text: (string)) -> ())
			table.insert(reliable_events[31], Callback)
			for _, value in reliable_event_queue[31] do
				task.spawn(Callback, value)
			end
			reliable_event_queue[31] = {}
			return function()
				table.remove(reliable_events[31], table.find(reliable_events[31], Callback))
			end
		end,
	},
	CreateCenterNotify = {
		On = function(Callback: (text: (string), showBar: (boolean)) -> ())
			table.insert(reliable_events[32], Callback)
			for _, value in reliable_event_queue[32] do
				task.spawn(Callback, unpack(value))
			end
			reliable_event_queue[32] = {}
			return function()
				table.remove(reliable_events[32], table.find(reliable_events[32], Callback))
			end
		end,
	},
	CreateFSNotify = {
		On = function(Callback: (text: (string), duration: (number)) -> ())
			table.insert(reliable_events[33], Callback)
			for _, value in reliable_event_queue[33] do
				task.spawn(Callback, unpack(value))
			end
			reliable_event_queue[33] = {}
			return function()
				table.remove(reliable_events[33], table.find(reliable_events[33], Callback))
			end
		end,
	},
	CreateCircularFade = {
		On = function(Callback: (duration: (number)) -> ())
			table.insert(reliable_events[34], Callback)
			for _, value in reliable_event_queue[34] do
				task.spawn(Callback, value)
			end
			reliable_event_queue[34] = {}
			return function()
				table.remove(reliable_events[34], table.find(reliable_events[34], Callback))
			end
		end,
	},
}
type Events = typeof(returns)
return returns
