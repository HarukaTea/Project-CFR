
local MAX_NUMBER = math.huge - 1599

return {
    LevelMin = 1,
    LevelMax = 999,

    TotalTargetingMin = 1,
    TotalTargetingMax = 999,


    --- stats
    DMGMin = 1,
    DMGMax = MAX_NUMBER,

    SpeedMin = 0,
    SpeedMax = 100,

    MaxHealthMin = 0,
    MaxHealthMax = MAX_NUMBER,

    EnvironmentBonusMin = 0.1,
    EnvironmentBonusMax = 999,
}
